#!/bin/bash
echo "✅ Script TIGO Ilimitado ejecutado correctamente."
set -e
export TZ="America/El_Salvador"
echo "🛠️ Instalando dependencias..."

# --- Actualizar índices de paquetes ---
sudo apt update -y
sudo apt upgrade -y

# --- Instalar paquetes necesarios ---
sudo apt install -y util-linux git cmake build-essential curl unzip wget \
python3 python3-dev python-is-python3 screen net-tools python3-pip locales cpulimit stunnel4

# --- Configurar locales ---
sudo sed -i '/es_SV.UTF-8/s/^# //g' /etc/locale.gen
sudo locale-gen
sudo update-locale LANG=es_SV.UTF-8
sudo timedatectl set-timezone America/El_Salvador

# ===============================
# 1. PROXY PYTHON EN PUERTO 80 (NOHUP)
# ===============================

echo "🌀 Configurando proxy Python en puerto 80..."

# Detener proxy si ya está corriendo
echo "🛑 Deteniendo proxy Python actual para evitar conflictos..."
pkill -f "PDirect.py 80" || true
sleep 2
fuser -k 80/tcp || true
sleep 2

mkdir -p /etc/ADMcgh

cat > /etc/ADMcgh/PDirect.py << 'EOF'
#!/usr/bin/env python3

import asyncio
import sys

LISTENING_ADDR = '0.0.0.0'
LISTENING_PORT = int(sys.argv[1]) if len(sys.argv) > 1 else 80
PASS = ''
BUFLEN = 4096 * 4
TIMEOUT = 60
DEFAULT_HOST = '127.0.0.1:22'
STATUS_RESP = '101'
FTAG = '\r\nContent-length: 0\r\n\r\nHTTP/1.1 200 Connection Established\r\n\r\n'
STATUS_TXT = '<strong style="color: #ff0000;">Kang Sae-byeok</strong>'
RESPONSE = f"HTTP/1.1 {STATUS_RESP} {STATUS_TXT} {FTAG}"
MAX_CONNECTIONS = 50
semaphore = asyncio.Semaphore(MAX_CONNECTIONS)

def find_header(header, data):
    try:
        data_str = data.decode(errors='ignore')
        start = data_str.find(header + ": ")
        if start == -1: return ""
        start += len(header) + 2
        end = data_str.find("\r\n", start)
        return data_str[start:end] if end != -1 else data_str[start:]
    except: return ""

async def connect_target(host_port):
    try:
        if ':' in host_port:
            host, port = host_port.split(':')
            port = int(port)
        else:
            host = host_port
            port = 22
        reader, writer = await asyncio.open_connection(host, port)
        return reader, writer
    except: return None, None

async def handle_client(client_reader, client_writer):
    async with semaphore:
        try:
            data = await asyncio.wait_for(client_reader.read(BUFLEN), timeout=TIMEOUT)
            if not data:
                client_writer.close()
                await client_writer.wait_closed()
                return

            host_port = find_header('X-Real-Host', data) or DEFAULT_HOST  
            passwd = find_header('X-Pass', data)  

            if PASS and passwd != PASS:  
                client_writer.write(b'HTTP/1.1 400 WrongPass!\r\n\r\n')  
                await client_writer.drain()  
                client_writer.close()  
                await client_writer.wait_closed()  
                return  
            elif host_port.startswith('127.0.0.1') or host_port.startswith('localhost') or not PASS:  
                target_reader, target_writer = await connect_target(host_port)  
                if not target_reader:  
                    client_writer.close()  
                    await client_writer.wait_closed()  
                    return  
                client_writer.write(RESPONSE.encode())  
                await client_writer.drain()  
                await asyncio.gather(  
                    proxy_data(client_reader, target_writer),  
                    proxy_data(target_reader, client_writer)  
                )  
        except Exception as e:  
            pass  
        finally:  
            client_writer.close()  
            await client_writer.wait_closed()

async def proxy_data(reader, writer):
    try:
        while True:
            data = await reader.read(BUFLEN)
            if not data: break
            writer.write(data)
            await writer.drain()
    except: pass
    finally:
        try:
            writer.close()
            await writer.wait_closed()
        except: pass

async def main(host=LISTENING_ADDR, port=LISTENING_PORT):
    server = await asyncio.start_server(handle_client, host, port)
    addrs = ', '.join(str(sock.getsockname()) for sock in server.sockets)
    print("🌐 PROXY HTTP MCCARTHEY - VPS TUNNEL")
    print("IP:", host)
    print("PORTA:", port)
    print("🔐 Powered by MCC-KEY System 💎")
    print(f"Listening on: {addrs}")
    async with server:  
        await server.serve_forever()

if __name__ == '__main__':
    try:
        asyncio.run(main())
    except KeyboardInterrupt:
        print("Parando servidor...")
EOF

chmod +x /etc/ADMcgh/PDirect.py

# ===============================
# 2. STUNNEL4: 443 → 80 (SSL)
# ===============================

echo "🔐 Generando certificado SSL autofirmado..."
mkdir -p /etc/stunnel/certs

if [ ! -f /etc/stunnel/certs/stunnel.pem ]; then
    openssl req -x509 -nodes -days 1095 -newkey rsa:2048 \
        -keyout /etc/stunnel/certs/stunnel.key \
        -out /etc/stunnel/certs/stunnel.crt \
        -subj "/C=SV/ST=San Salvador/L=Santa Ana/O=McCartheyVPN/OU=McCPanel/CN=SSLService/emailAddress=admin@mccarthey.net"

    cat /etc/stunnel/certs/stunnel.key /etc/stunnel/certs/stunnel.crt > /etc/stunnel/certs/stunnel.pem
    chmod 600 /etc/stunnel/certs/stunnel.pem
    echo "✅ Certificado generado."
else
    echo "✅ Certificado ya existe."
fi

echo "🧩 Configurando Stunnel..."
cat > /etc/stunnel/stunnel.conf <<EOF
cert = /etc/stunnel/certs/stunnel.pem
pid = /var/run/stunnel4/stunnel4.pid
setuid = root
setgid = root
debug = 3
client = no
foreground = no
socket = l:TCP_NODELAY=1
socket = r:TCP_NODELAY=1

[python-https]
accept = 0.0.0.0:443
connect = 127.0.0.1:80
TIMEOUTclose = 0
EOF

mkdir -p /var/run/stunnel4
chown root:root /var/run/stunnel4
chmod 755 /var/run/stunnel4

systemctl enable stunnel4
systemctl restart stunnel4

# Iniciar proxy Python en localhost:80
echo "🔄 Iniciando proxy Python en 127.0.0.1:80..."
pkill -f "PDirect.py 80" || true
sleep 2
nohup python3 /etc/ADMcgh/PDirect.py 80 > /root/nohup.out 2>&1 &

# ===============================
# 3. BADVPN: 7200 y 7300
# ===============================

echo "📡 Compilando BadVPN..."
rm -rf /opt/badvpn
git clone https://github.com/ambrop72/badvpn.git /opt/badvpn
mkdir -p /opt/badvpn/build && cd /opt/badvpn/build
cmake .. -DBUILD_NOTHING_BY_DEFAULT=1 -DBUILD_UDPGW=1
make -j$(nproc)

pkill -f badvpn-udpgw || true
cp udpgw/badvpn-udpgw /usr/bin/
chmod +x /usr/bin/badvpn-udpgw

screen -S badvpn -X quit || true
screen -S badUDP72 -X quit || true

screen -dmS badvpn /usr/bin/badvpn-udpgw --listen-addr 127.0.0.1:7300 --max-clients 1000 --max-connections-for-client 10
screen -dmS badUDP72 /usr/bin/badvpn-udpgw --listen-addr 127.0.0.1:7200 --max-clients 1000 --max-connections-for-client 10

# ===============================
# 4. AUTOINICIO rc.local
# ===============================

echo "🧩 Configurando rc.local..."
cat > /etc/rc.local <<EOF
#!/bin/bash
sleep 15
nohup python3 /etc/ADMcgh/PDirect.py 80 > /root/nohup.out 2>&1 &
systemctl start stunnel4
screen -dmS badvpn /usr/bin/badvpn-udpgw --listen-addr 127.0.0.1:7300 --max-clients 1000 --max-connections-for-client 10
screen -dmS badUDP72 /usr/bin/badvpn-udpgw --listen-addr 127.0.0.1:7200 --max-clients 1000 --max-connections-for-client 10
exit 0
EOF

chmod +x /etc/rc.local

cat > /etc/systemd/system/rc-local.service <<EOF
[Unit]
Description=/etc/rc.local Compatibility
ConditionPathExists=/etc/rc.local

[Service]
Type=forking
ExecStart=/etc/rc.local start
TimeoutSec=0
StandardOutput=tty
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable rc-local
systemctl restart rc-local

# ===============================
# 5. GESTIÓN DE RECURSOS
# ===============================

echo "🛠️ Configurando gestión de RAM y CPU..."
touch /var/log/gestionar_recursos.log
chmod 644 /var/log/gestionar_recursos.log

cat > /root/gestionar_recursos.sh << 'EOF'
#!/bin/bash
LOCK_FILE="/tmp/gestionar_recursos.lock"
LOG_FILE="/var/log/gestionar_recursos.log"
PID_FILE="/tmp/cpulimit_pids.txt"

[ -f "$LOCK_FILE" ] && { echo "$(date): Ya en ejecución" >> "$LOG_FILE"; exit 1; }
touch "$LOCK_FILE"
trap 'rm -f "$LOCK_FILE"' INT TERM EXIT

cleanup_cpulimit() {
    [ -f "$PID_FILE" ] && while read -r cpid; do kill -9 "$cpid" 2>/dev/null; done < "$PID_FILE"
    > "$PID_FILE"
    pkill -9 -f "cpulimit -p" 2>/dev/null
}

cleanup_cpulimit

pgrep -f "/root/scrip.sh mon" > /dev/null || { /bin/bash /root/scrip.sh mon & echo "$(date): Iniciado scrip.sh mon" >> "$LOG_FILE"; }

MEM_THRESHOLD=200
CACHE_THRESHOLD=100
LOAD_THRESHOLD=2.0
PREV_PIDS_PDIRECT="" PREV_PIDS_BADVPN="" PREV_PIDS_SCRIP=""

while true; do
    FREE_MEM=$(free -m | awk '/Mem:/ {print $4}')
    CACHE_MEM=$(free -m | awk '/Mem:/ {print $6}')

    if [ "$FREE_MEM" -lt "$MEM_THRESHOLD" ] || [ "$CACHE_MEM" -gt "$CACHE_THRESHOLD" ]; then
        echo "$(date): Liberando caché..." >> "$LOG_FILE"
        sync; sudo sysctl -w vm.drop_caches=3 > /dev/null
    fi

    LOAD_AVG=$(awk '{print $1}' /proc/loadavg)
    if (( $(echo "$LOAD_AVG > $LOAD_THRESHOLD" | bc -l) )); then
        CURRENT_PIDS_PDIRECT=$(pgrep -f /etc/ADMcgh/PDirect.py)
        CURRENT_PIDS_BADVPN=$(pgrep -f badvpn-udpgw)
        CURRENT_PIDS_SCRIP=$(pgrep -f "/root/scrip.sh mon")

        if [ "$CURRENT_PIDS_PDIRECT" != "$PREV_PIDS_PDIRECT" ] || [ "$CURRENT_PIDS_BADVPN" != "$PREV_PIDS_BADVPN" ] || [ "$CURRENT_PIDS_SCRIP" != "$PREV_PIDS_SCRIP" ]; then
            cleanup_cpulimit
            for pid in $CURRENT_PIDS_PDIRECT; do cpulimit -p "$pid" -l 20 & echo $! >> "$PID_FILE"; done
            for pid in $CURRENT_PIDS_BADVPN; do cpulimit -p "$pid" -l 60 & echo $! >> "$PID_FILE"; done
            for pid in $CURRENT_PIDS_SCRIP; do cpulimit -p "$pid" -l 10 & echo $! >> "$PID_FILE"; done
            PREV_PIDS_PDIRECT="$CURRENT_PIDS_PDIRECT"
            PREV_PIDS_BADVPN="$CURRENT_PIDS_BADVPN"
            PREV_PIDS_SCRIP="$CURRENT_PIDS_SCRIP"
        fi
    fi
    sleep 10
done
EOF

chmod +x /root/gestionar_recursos.sh

cat > /etc/systemd/system/gestionar-recursos.service <<EOF
[Unit]
Description=Gestiona RAM y CPU
After=network.target rc-local.service stunnel4.service

[Service]
Type=simple
User=root
ExecStart=/bin/bash /root/gestionar_recursos.sh
Restart=always
RestartSec=5s

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable gestionar-recursos.service
systemctl restart gestionar-recursos.service

# ===============================
# 6. MENSAJE FINAL
# ===============================

echo ""
echo "======================================="
echo "✅ INSTALACIÓN COMPLETA MCCARTHY VPN"
echo "📦 Puertos activos:"
ss -tulnp | grep -E ':443|:80|:7200|:7300' || true
echo ""
echo "🔒 SSL en 443 → 80 (Stunnel4)"
echo "🌐 Proxy Python en 127.0.0.1:80"
echo "🎯 SNI: SSLService | Puerto: 443"
echo "💎 Firma: Kang Sae-byeok"
echo "🛠️ Gestión de recursos activa"
echo "======================================="
echo ""

exit 0
